#version: '3.8'
#
#services:
#  # ──────────────────────────────────────────────────
#  # ▶ PostGIS (PostgreSQL + PostGIS 확장)
#  # ──────────────────────────────────────────────────
#  postgis:
#    image: postgis/postgis:17-3.5
#    container_name: postgis
#    platform: linux/amd64
#    ports:
#      - "5432:5432"
#    environment:
#      - POSTGRES_USER=sae1013
#      - POSTGRES_PASSWORD=1234
#      - POSTGRES_DB=restroom
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#    # healthcheck (선택) - Postgres 가 기동됐는지 확인
#    healthcheck:
#      test: [ "CMD-SHELL", "pg_isready -U sae1013" ]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#
#  # ──────────────────────────────────────────────────
#  # ▶ Redis
#  # ──────────────────────────────────────────────────
#  redis:
#    image: redis:latest
#    container_name: redis
#    ports:
#      - "6379:6379"
#    # restart: always
#    healthcheck:
#      test: [ "CMD", "redis-cli", "ping" ]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#
#  # ──────────────────────────────────────────────────
#  # ▶ NestJS WAS (modoo_toilet_server)
#  # ──────────────────────────────────────────────────
#  webapp:
#    image: minwoojungdev/haewooso:latest
#    container_name: haewooso_app
#    # postgis, redis 컨테이너가 “정상(healthy)” 상태가 될 때까지 기다리도록 설정
#    depends_on:
#      postgis:
#        condition: service_healthy
#      redis:
#        condition: service_healthy
#    # .env 파일을 읽어서 환경변수 주입
#    env_file:
#      - .env
#    environment:
#      # .env 내부의 DB_HOST=localhost → postgis 로 덮어쓰기
#      - DB_HOST=postgis
#      - DB_PORT=5432
#      - DB_USERNAME=sae1013
#      - DB_PASSWORD=1234
#      - DB_DATABASE=restroom
#
#      # .env 내부의 REDIS_DEV_URL 값은 컨테이너 네트워크용으로 재정의
#      - REDIS_DEV_URL=redis://redis:6379
#      - REDIS_PORT=6379
#
#      # NestJS는 process.env.WAS_PORT 를 읽어서 바인딩 (Dockerfile에서 EXPOSE 8000)
#      - WAS_PORT=8000
#
#      # 그 외 .env에 포함된 값은 env_file 로 자동 주입됨
#      # - SESSION_PWD=…
#      # - JWT_SECRET=…
#      # - CACHE_TTL=…
#      # - COOL_SMS_API_KEY=…
#      # - COOL_SMS_API_SECRET=…
#      # - GMAIL_USER=…
#      # - GMAIL_PASSWORD=…
#      # (env_file:.env 아래에서 자동으로 읽어 들여집니다)
#
#    ports:
#      - "8000:8000"
#    # volumes: # 필요 시 호스트 디렉터리를 마운트 (예: 로그, 업로드 등)
#    #   - ./logs:/usr/src/app/logs
#
#    # (선택) 재시작 정책
#    restart: on-failure
#
#volumes:
#  pgdata:
